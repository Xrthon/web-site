<div class="mx-auto w-full px-6">
  <div class="overflow-hidden rounded-3xl bg-white shadow-xl">

    <!-- HEADER -->
    <div class="bg-gradient-to-r from-gray-900 to-gray-700 px-8 py-10">
      <h1 class="text-3xl font-bold text-white">
        Mot de passe oublié
      </h1>

      <p class="mt-2 text-gray-300">
        Demandez un lien sécurisé pour réinitialiser votre mot de passe.
      </p>
    </div>

    <div class="p-8">

      <% if alert = flash[:alert] %>
        <div class="mb-6 rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm font-medium text-red-700">
          <%= alert %>
        </div>
      <% end %>

      <% if notice = flash[:notice] %>
        <div class="mb-6 rounded-xl border border-green-200 bg-green-50 px-4 py-3 text-sm font-medium text-green-700">
          <%= notice %>
        </div>
      <% end %>

      <p class="mb-6 text-sm text-gray-600">
        Entrez l’adresse courriel associée à votre compte. Si elle existe,
        un lien de réinitialisation sera envoyé.
      </p>

      <%= form_with url: passwords_path,
                    method: :post,
                    data: { turbo: false },
                    class: "space-y-6" do |form| %>

        <!-- EMAIL -->
        <div>
          <%= form.label :email,
              "Adresse courriel",
              class: "mb-2 block text-sm font-medium text-gray-700" %>

          <%= form.email_field :email,
              required: true,
              autocomplete: "email",
              placeholder: "exemple@email.com",
              class: "w-full rounded-xl border-gray-300 px-4 py-3" %>
        </div>

        <!-- SUBMIT -->
        <div class="pt-4">
          <%= form.submit "Envoyer le lien de réinitialisation",
              class: "
                w-full rounded-xl bg-gray-900 px-6 py-3
                font-medium text-white transition
                hover:bg-gray-800
              " %>
        </div>

      <% end %>

      <div class="mt-6 text-center">
        <%= link_to "Retour à la connexion",
            new_session_path,
            class: "text-sm font-medium text-gray-700 hover:text-gray-900" %>
      </div>

    </div>

  </div>
</div>